"""Backward-compatibility shims.

This package previously contained the full legacy (Chroma-backed) runtime.
As of v1.0, the canonical runtime is the SQLite/FTS5 service runtime.
The compat package is kept as a namespace but contains no active code.
"""
